name: Pre-commit checks

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: '3.14'

      - uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4
        with:
          terraform_version: "1.14.3"

      - name: Install tflint
        run: |
          # renovate: datasource=github-releases depName=terraform-linters/tflint
          VERSION=0.64.0
          ARCH=$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/')
          # Renovate's github-releases datasource writes the version without the "v" prefix, while the tflint release tag requires it (tag "v0.64.0").
          curl -sSLo ./tflint.zip "https://github.com/terraform-linters/tflint/releases/download/v${VERSION}/tflint_linux_${ARCH}.zip"
          unzip tflint.zip tflint
          chmod +x tflint
          mv tflint /usr/local/bin/tflint
          rm tflint.zip

      - name: Install terraform-docs
        run: |
          # renovate: datasource=github-releases depName=terraform-docs/terraform-docs extractVersion=^v(?<version>.+)$
          VERSION=0.24.0
          ARCH=$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/')
          curl -sSLo ./terraform-docs.tar.gz "https://terraform-docs.io/dl/v${VERSION}/terraform-docs-v${VERSION}-$(uname)-${ARCH}.tar.gz"
          tar -xzf terraform-docs.tar.gz terraform-docs
          chmod +x terraform-docs
          mv terraform-docs /usr/local/bin/terraform-docs
          rm terraform-docs.tar.gz

      # Note: collections will be installed to /opt/pipx/venvs/ansible-core/lib/python3.12/site-packages/ansible_collections (why?)
      - name: Install Ansible requirements
        run: cd ansible && ansible-galaxy install -vv -r requirements.yml

      - name: Pre-commit checks
        uses: pre-commit/action@2c7b3805fd2a0fd8c1884dcaebf91fc102a13ecd # v3.0.1
