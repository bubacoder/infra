# Minimal host-specific Docker Compose settings for a local core deployment.
# Set CLOUDFLARE_DNS_API_TOKEN in the ignored .env.traefik file for this host.

DOCKER_VOLUMES=/srv/docker-volumes
CROWDSEC_ENABLED=false
