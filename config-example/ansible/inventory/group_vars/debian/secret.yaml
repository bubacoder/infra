---

# Leave empty for the default SSH-key-only administrator. If configuring a
# password, also set a unique salt (maximum 16 characters).
admin_password: ""
password_salt: ""

# Set only when the CrowdSec bouncer is enabled.
debian_base_crowdsec_local_api_key: ""
